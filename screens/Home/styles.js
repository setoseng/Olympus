import { colors } from '../../theme'
import { texts } from '../../theme'


export const styles = {
  container: {
    flex: 1,
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'center',
    alignItems: 'center',
  },
  directoryButton: {
    margin: 5,
    width: 200,
    height: 150,
    backgroundColor: colors.primary,
    justifyContent: 'center',
    borderRadius: 30,
  }
}